<p id="notice"><%= notice %></p>

<p>
  <strong>User:</strong>
  <%= @selection.user_id %>
</p>

<p>
  <strong>Item:</strong>
  <%= @selection.item_id %>
</p>

<%= link_to 'Edit', edit_selection_path(@selection) %> |
<%= link_to 'Back', selections_path %>
